Проверить все ли пользователи старше 10 лет
 
Подсказка:
<div class="hint">
allMatch()
</div>

Ответ:
<div class="hint">
  allMatch(u -> u.getAge() > 10);
</div>
                                        