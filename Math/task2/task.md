Вычислить самое длинное имя (firstName)

<div class="hint">
mapToInt()
</div>

Варианты решений:
<div class="hint">
  mapToInt(u -> u.getFirstName().length())

.max().getAsInt();

Вариант II:

.summaryStatistics().getMax();

</div>
                                        